# Changesets

This repository publishes two npm packages from one monorepo:

- `fortheagent`
- `fortheagent-cli`

Both packages version together. Add a changeset for any user-visible change that
should be published.
