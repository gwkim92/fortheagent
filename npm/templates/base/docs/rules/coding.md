# Coding Rules

- Preserve managed files unless the package contract explicitly allows changes.
- Prefer small, reviewable changes.
- Run validation commands before claiming structural work is complete.
